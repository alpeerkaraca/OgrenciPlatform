@using System.Security.Claims
@model Shared.DTO.AdvisedStudentsDTO
@{
    ViewBag.Title = "Danışan Öğrenciler";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row mt-2">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-people-fill me-2"></i>Danışan Öğrenciler
                    </h4>
                    @if (User.Identity.IsAuthenticated)
                    {
                        var claims = (ClaimsIdentity)User.Identity;
                        var advisorName = claims.FindFirst("full_name").Value;
                        <small class="opacity-75">Danışman: @advisorName</small>
                    }
                </div>
                <div class="card-body">
                    @if (Model.AdvisedStudents is { Count: > 0 })
                    {
                        <div class="table-responsive">
                            <table class="table table-striped table-hover" id="studentsTable">
                                <thead class="table-dark">
                                <tr>
                                    <th>Öğrenci No</th>
                                    <th>Ad Soyad</th>
                                    <th>E-posta</th>
                                    <th>Bölüm</th>
                                    <th>Kayıt Tarihi</th>
                                    <th class="text-center">İşlemler</th>
                                </tr>
                                </thead>
                                <tbody>
                                @foreach (var student in Model.AdvisedStudents)
                                {
                                    <tr>
                                        <td>
                                            <strong class="text-primary">@student.StudentNo</strong>
                                        </td>
                                        <td>@student.StudentFullName</td>
                                        <td>
                                            <a href="mailto:@student.StudentEmail" class="text-decoration-none">
                                                <i class="bi bi-envelope me-1"></i>@student.StudentEmail
                                            </a>
                                        </td>
                                        <td>
                                            @if (student.StudentDepartmentName != null)
                                            {
                                                <span class="badge bg-info">@student.StudentDepartmentName</span>
                                            }
                                        </td>
                                        <td>@student.StudentCreateDate.ToString("dd.MM.yyyy")</td>
                                        <td class="text-center">
                                            <a href="@Url.Action("StudentDetail", "Advisor", new { id = student.StudentId })"
                                               class="btn btn-sm btn-outline-primary" title="Öğrenci Detayı">
                                                <i class="bi bi-eye"></i> Detay
                                            </a>
                                        </td>
                                    </tr>
                                }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-info text-center">
                            <i class="bi bi-info-circle-fill me-2"></i>
                            Henüz Danışan Öğrenciniz bulunmamaktadır.
                        </div>
                    }
                </div>
                @if (Model != null && Model.AdvisedStudents.Count > 0)
                {
                    <div class="card-footer bg-light">
                        <div class="row">
                            <div class="col-sm-6">
                                <small class="text-muted">
                                    <i class="bi bi-person-check me-1"></i>
                                    Toplam @Model.AdvisedStudents.Count() Öğrenci
                                </small>
                            </div>
                            <div class="col-sm-6 text-end">
                                <small class="text-muted">
                                    <i class="bi bi-clock me-1"></i>
                                    Son güncelleme: @DateTime.Now.ToString("dd.MM.yyyy HH:mm")
                                </small>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>

        document.addEventListener('DOMContentLoaded',
            function() {
                const table = document.getElementById('studentsTable');

                if (table && typeof $.fn.DataTable !== 'undefined') {
                    $('#studentsTable').DataTable({
                        language: {
                            url: '/Content/datatables/i18n/tr.json'
                        },
                        responsive: true,
                        pageLength: 25,
                        order: [[0, 'asc']],
                        columnDefs: [
                            { orderable: false, targets: [5] },
                            { className: "text-center", targets: [5] }
                        ]
                    });
                }
            });
    </script>
}