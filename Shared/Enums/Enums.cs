namespace Shared.Enums
{
    public enum Roles
    {
        Admin = 1,
        Danışman = 2,
        Öğrenci = 3,
    }

    public enum ApprovalStatus
    {
        Bekliyor = 0,
        Onaylandı = 1,
        Reddedildi = 2,
    }

    public enum DaysOfWeek
    {
        Pazartesi = 1,
        Salı = 2,
        Çarşamba = 3,
        Perşembe = 4,
        Cuma = 5,
        Cumartesi = 6,
        Pazar = 7,
    }
}